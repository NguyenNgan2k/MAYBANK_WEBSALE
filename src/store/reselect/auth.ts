import { AppState } from "../reducers";

export const getSuccessful = (state: AppState) => state.auth.successful;

/************************************ MAKE GET MESSAGE ***********************************/
